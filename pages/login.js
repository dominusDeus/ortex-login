// import {useState} from 'react';

const Login = () => {
  // const [user, setUser] = useState(currentUser);
  // console.log(currentUser);
  debugger;
  // console.log(user);
  // const {email, password} = user;
  // console.log(email, password);
  // return null;
  return (
    // <div>holi este es tu mail {email} y este es tu password {password}</div>
    <div>You logged in!</div>
  )

    // const axiosRes = await axios.get(`${process.env.ROOT_URI}/api/loginapi`);
    // const login = await axiosRes.data;
    // console.log(login[0].email);

}

export default Login;